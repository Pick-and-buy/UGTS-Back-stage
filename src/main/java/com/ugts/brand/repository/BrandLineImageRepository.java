package com.ugts.brand.repository;

import com.ugts.brand.entity.BrandLineImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BrandLineImageRepository extends JpaRepository<BrandLineImage, String> {
}
